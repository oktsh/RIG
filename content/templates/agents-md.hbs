# AGENTS.md

> Auto-generated by RIG. Edit rig.toml, then run `rig generate`.

## Project: {{project.name}}

### Preset: {{presetMeta.displayName}}

{{presetMeta.description}}

## Agents

{{#each agents}}
### {{name}}

{{description}}

- **Model:** {{model}}
- **Memory:** {{memory}}
- **Read:** {{#each file_ownership.read}}`{{this}}`{{#unless @last}}, {{/unless}}{{/each}}
- **Write:** {{#each file_ownership.write}}`{{this}}`{{#unless @last}}, {{/unless}}{{/each}}

{{/each}}

## Rules

{{#each rules}}
- **{{id}}**: {{filename}}
{{/each}}

## Quality Gates

{{#is presetMeta.id "pm"}}
Simplified: plan → build → review → ship
{{/is}}
{{#is presetMeta.id "small-team"}}
Full pipeline: lint → typecheck → test → fresh code-reviewer → max 3 iterations
{{/is}}
{{#is presetMeta.id "solo-dev"}}
Standard: lint + typecheck hooks, code-reviewer on demand
{{/is}}
