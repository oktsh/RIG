## [GYRD-MANAGED] Agents

{{#each agents}}
- **{{name}}** ({{model}}) — {{description}}
{{/each}}

Use agents by saying: "use [agent-name] agent" (e.g., "use code-reviewer agent").
Agent definitions: `.claude/agents/`
